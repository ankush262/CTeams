/**
 * Enable React Strict Mode to help identify potential problems
 * during development by highlighting unsafe lifecycle methods,
 * deprecated APIs, and other side-effect issues.
 */
const nextConfig = {
  reactStrictMode: true,
};

module.exports = nextConfig;
